import styled from "styled-components";
import { Button, Card } from "antd";

export const CreateFilmsContainer = styled(Card)`
  margin: 0 auto;
  max-width: 1200px;
  display: flex;
  flex-direction: column;
  align-items: center;
`;
